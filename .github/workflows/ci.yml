name: CI – Автотесты Django

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.12]

    steps:
    - uses: actions/checkout@v4
    - name: Установка Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Установка зависимостей
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-django pytest-cov
    - name: Запуск тестов с покрытием
      run: |
        pytest --cov=. --cov-report=xml --cov-omit="*/migrations/*,*/tests/*,*/admin.py,*/apps.py,*/__init__.py,manage.py,main.py,kosti/asgi.py,kosti/wsgi.py,*/templatetags/*,*/signals.py"
    - name: Загрузка отчёта о покрытии (Codecov)
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
